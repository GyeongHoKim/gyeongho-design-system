name: CI

on:
  # Run on every PR regardless of base branch (stacked/release branches included),
  # so the lint/build/test gate is never skipped.
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  # Only cancel superseded PR runs. Never cancel a push-to-main run, or a main
  # commit could be left without a green required check.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verify:
    name: lint · build · test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup toolchain
        uses: ./.github/actions/setup

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test

      # Storybook interaction/a11y tests (@storybook/addon-vitest) run in a real
      # browser via Playwright — install only the Chromium binary the 3 storybook
      # apps' vitest.config.ts request, not the full Playwright browser matrix.
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Test Storybook
        run: pnpm test:storybook
