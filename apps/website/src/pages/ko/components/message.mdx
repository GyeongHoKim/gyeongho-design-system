---
layout: ../../../layouts/MarkdownLayout.astro
title: Message
description: 아바타, 작성자, 타임스탬프, 말풍선을 조합하는 채팅 메시지 행입니다.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import MessageDemoReact from '../../../components/demos/MessageDemo.tsx';
import { withBase } from '../../../lib/withBase';

`Message`와 그 하위 컴포넌트는 **React 전용**이에요(`@ghds/react`). 아직 Web Components나 React Native 대응물은 없어요.

## 개요

`Message`는 채팅 메시지 행이에요: 선택적 아바타, 그리고 작성자 + 타임스탬프 헤더와 `Bubble`이 있는 콘텐츠 열. `side` prop이 레이아웃을 뒤집어 발신 메시지가 끝에 정렬돼요. `MessageAvatar`, `MessageContent`, `MessageAuthor`, `MessageTimestamp`로 조합하고, `Bubble`(`@ghds/react/bubble`에서)과 `Avatar`(`@ghds/react/avatar`에서)를 함께 쓰세요.

## 구조

다음과 같이 배치된 flex 행이에요: `MessageAvatar`(선행 `Avatar`)와 `MessageContent` — 헤더(`MessageAuthor` + `MessageTimestamp`)를 `Bubble` 위에 담는 열. `side="sent"`는 행을 뒤집어 아바타와 말풍선이 끝에 정렬돼요. 간격과 텍스트 색상은 `@ghds/tokens`(`comp.message.*`)에서 와요.

## 변형 및 상태

유일한 축은 `side`(`received`는 시작에, `sent`는 끝에 정렬)예요. 말풍선이 시각적 변형을 가져요.

<StateList
  states={[
    { name: 'received', present: true, note: '시작에 정렬(수신).' },
    { name: 'sent', present: true, note: '끝에 정렬(발신).' },
    { name: 'Hover / Focus', present: false, note: '행은 정적이에요; 인터랙티브 컨트롤은 자체 상태를 설정해요.' },
    { name: '비활성화', present: false },
    { name: '로딩', present: false },
  ]}
/>

## 사용법

<DoDont
  do={[
    'side를 방향에 맞추세요: 수신에는 received, 현재 사용자에는 sent.',
    'Bubble의 variant를 side와 짝지으세요(received 행에는 received 말풍선, sent에는 sent).',
    'sent 행에서 헤더와 말풍선이 가장자리에 붙도록 MessageContent에 align-items: flex-end를 주세요.',
  ]}
  dont={[
    "원시 div로 메시지 행을 직접 만들지 마세요 — 하위 컴포넌트가 간격과 텍스트 토큰을 가져요.",
    "메시지를 누가 보냈는지 색상만으로 전달하지 마세요; 작성자 이름을 계속 보이게 유지하세요.",
  ]}
/>

## 접근성

- 작성자 이름(`MessageAuthor`)이 화자를 명확히 하므로, 정체성이 아바타나 말풍선 색상만에 의존하지 않아요.
- `Avatar`에 `name`을 제공해 접근 가능한 라벨 / 이니셜 폴백을 갖게 하세요.
- 전체 로그의 경우, 순서가 보조 기술에 전달되도록 메시지를 `MessageScroller`(또는 list/log 랜드마크)로 감싸세요.
- 공통 관례는 <a href={withBase('/accessibility/')}>접근성 가이드</a>를 참고하세요.

## 콘텐츠

헤더는 짧은 작성자 이름과 간결한 타임스탬프로 유지하세요. 메시지 텍스트는 `Bubble`에 있어요; 그 안에 작성자를 반복하지 마세요.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'side', type: "'received' | 'sent'", default: "'received'", description: "'received'는 시작에, 'sent'는 끝에 정렬(Message)." },
        { name: '...rest', type: 'HTMLAttributes<HTMLDivElement>', description: '네이티브 <div> 속성이 행으로 전달돼요.' },
        { name: 'MessageAvatar / MessageContent', type: 'HTMLAttributes<HTMLDivElement>', description: '아바타 슬롯과 헤더 + 말풍선 열.' },
        { name: 'MessageAuthor / MessageTimestamp', type: 'HTMLAttributes<HTMLSpanElement>', description: '헤더의 작성자 이름과 타임스탬프.' },
      ],
    },
  ]}
/>

## 라이브 데모

아래에서 React가 짧은 수신/발신 대화를 렌더해요 — 헤더에서 다크 모드를 토글하면 행과 말풍선이 반응해요.

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <MessageDemoReact client:only="react" />
  </div>
</div>

```tsx
import { Message, MessageAvatar, MessageContent, MessageAuthor, MessageTimestamp } from '@ghds/react/message';
import { Bubble } from '@ghds/react/bubble';
import { Avatar } from '@ghds/react/avatar';

<Message side="received">
  <MessageAvatar>
    <Avatar name="Bo Ram" />
  </MessageAvatar>
  <MessageContent>
    <MessageAuthor>Bo Ram</MessageAuthor>
    <MessageTimestamp>10:02</MessageTimestamp>
    <Bubble variant="received">Hey — are we still on for tomorrow?</Bubble>
  </MessageContent>
</Message>;
```
