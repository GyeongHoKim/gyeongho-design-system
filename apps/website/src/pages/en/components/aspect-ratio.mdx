---
layout: ../../../layouts/MarkdownLayout.astro
title: AspectRatio
description: A layout primitive that constrains its content to a fixed width-to-height ratio.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import AspectRatioDemoReact from '../../../components/demos/AspectRatioDemo.tsx';
import AspectRatioDemoLit from '../../../components/demos/AspectRatioDemo.astro';
import { withBase } from '../../../lib/withBase';

## Overview

An `AspectRatio` reserves a box at a fixed width-to-height ratio using the native CSS `aspect-ratio`
property — the classic use is holding responsive media (an image, a video, a map embed) so the
layout doesn't reflow while the asset loads. It fills the available width and derives its height
from the `ratio`.

## Anatomy

A single `<div>` set to `width: 100%`, the given `aspect-ratio`, and `overflow: hidden`. Size a
single child to `width: 100%; height: 100%` (e.g. an `<img>` with `object-fit: cover`) to fill the
box. There is no sketch layer — this primitive is purely structural.

## Variants & States

AspectRatio is a **static layout box** — it has no interactive states. The only variation is the
`ratio` value itself.

<StateList
  states={[
    { name: 'Hover', present: false, note: 'Not interactive.' },
    { name: 'Focus', present: false, note: 'Not focusable.' },
    { name: 'Disabled', present: false },
    { name: 'Loading', present: false },
    { name: 'Error', present: false },
  ]}
/>

## Usage

<DoDont
  do={[
    'Place a single child sized to width: 100%; height: 100% (an <img> with object-fit: cover is the common case).',
    'Pass the ratio as a number expression like 16 / 9 or 4 / 3 so intent stays readable.',
    'Constrain the outer width (e.g. a max-width wrapper) — the box derives its height from that width.',
  ]}
  dont={[
    "Don't set an explicit height on the box — the ratio derives it from the width.",
    "Don't use it purely to add spacing; it's for reserving media proportions, not general layout gaps.",
  ]}
/>

## Accessibility

- Renders a plain `<div>` with no role or label — it is a layout wrapper, so the accessible content
  is whatever child you place inside (e.g. an `<img>` needs its own `alt`).
- Because it reserves space before media loads, it prevents layout shift — a real accessibility and
  usability win for users on slow connections.
- See the <a href={withBase('/accessibility/')}>Accessibility Guide</a> for shared conventions.

## Content

N/A — AspectRatio has no fixed content model. Content is whatever single element you place inside,
sized to fill the box.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'ratio', type: 'number', default: '1', description: 'Desired width-to-height ratio, e.g. 16 / 9 for widescreen or 1 for a square.' },
        { name: '...rest', type: 'HTMLAttributes<HTMLDivElement>', description: 'Native <div> attributes (className, style, etc.) pass through.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'ratio', type: 'number', default: '1', description: 'Reflected attribute; sets the host aspect-ratio. Slot a single child to fill the box.' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'ratio', type: 'number', default: '1', description: 'Width-to-height ratio applied via the native aspectRatio style.' },
        { name: 'children', type: 'ReactNode', description: 'Content constrained to the ratio box.' },
        { name: 'style', type: 'StyleProp<ViewStyle>', description: 'Extra style merged onto the ratio box.' },
        { name: 'accessibilityLabel', type: 'string', description: 'Accessible label for the region.' },
        { name: 'testID', type: 'string', description: 'Test handle for queries.' },
      ],
    },
  ]}
/>

## Code examples

```tsx
// React — @ghds/react
import { AspectRatio } from '@ghds/react/aspect-ratio';

<AspectRatio ratio={16 / 9}>
  <img src="https://example.com/cover.jpg" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
</AspectRatio>;
```

```html
<!-- Web Components — @ghds/web-components -->
<script type="module">
  import '@ghds/web-components/aspect-ratio';
</script>
<gh-aspect-ratio ratio="1.7778">
  <img src="https://example.com/cover.jpg" style="width: 100%; height: 100%; object-fit: cover;" />
</gh-aspect-ratio>
```

```tsx
// React Native — @ghds/react-native
import { AspectRatio } from '@ghds/react-native/aspect-ratio';
import { Image } from 'react-native';

<AspectRatio ratio={16 / 9}>
  <Image
    source={{ uri: 'https://example.com/cover.jpg' }}
    style={{ width: '100%', height: '100%' }}
    resizeMode="cover"
  />
</AspectRatio>;
```

## Live Demo

React and Web Components render live below, driven by the same tokens — toggle dark mode in the
header to see both respond identically. React Native can't run in a browser without additional
`react-native-web` wiring (not set up in this site), so its usage is shown as a code sample instead.

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <AspectRatioDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <AspectRatioDemoLit />
  </div>
</div>
