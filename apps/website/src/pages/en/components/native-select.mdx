---
layout: ../../../layouts/MarkdownLayout.astro
title: NativeSelect
description: A hand-drawn box around a real native select, with a chevron and an error state.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import NativeSelectDemoReact from '../../../components/demos/NativeSelectDemo.tsx';
import NativeSelectDemoLit from '../../../components/demos/NativeSelectDemo.astro';
import { withBase } from '../../../lib/withBase';

## Overview

`NativeSelect` wraps a real native `<select>` in a hand-drawn box with a chevron. Unlike `Select`
(a fully custom listbox), it keeps the platform's native dropdown — which is the right call on touch
devices, for very long option lists, and when you want the OS's native picker. Pass `<option>`
children exactly as you would to a bare `<select>`.

## Anatomy

A Radix `Label` above a sketchy box containing the native `<select>` (its own border and arrow
removed via `appearance: none`) and an overlaid chevron icon. When an `error` is set, the field is
marked invalid and the message is rendered in a `role="alert"`. Every color, padding, radius, and
sketch parameter comes from `@ghds/tokens` (`comp.nativeSelect.*`).

## Variants & States

No named variants — the field reacts to focus, and `disabled` / `error` set its state.

<StateList
  states={[
    { name: 'Focus', present: true, note: 'Focus switches the outline to comp.nativeSelect.stroke.focus.' },
    { name: 'Error / invalid', present: true, note: 'Setting error paints the danger stroke and announces the message.' },
    { name: 'Disabled', present: true },
    { name: 'Hover', present: false },
    { name: 'Loading', present: false },
  ]}
/>

## Usage

<DoDont
  do={[
    'Prefer NativeSelect over the custom Select on touch devices and for long option lists.',
    'Pass a leading placeholder <option value=""> when there is no sensible default choice.',
    'Set error together with client-side validation so the danger stroke is explained by the message.',
  ]}
  dont={[
    "Don't reach for NativeSelect when you need rich, non-text options or custom option rendering — use Select.",
    "Don't rely on the danger stroke alone to convey an error — the error text should say what's wrong.",
  ]}
/>

## Accessibility

- A Radix `Label` is associated with the `<select>` via `htmlFor` / `id`, so the control has a real
  accessible name and the label is clickable.
- Error state is exposed with `aria-invalid` and `aria-describedby` pointing at a `role="alert"`
  message, so screen readers announce both the invalid state and the reason.
- Because the underlying element is a real `<select>`, all native keyboard behavior and the OS
  picker come for free.
- The chevron is decorative (`pointer-events: none`) and never intercepts interaction.
- See the <a href={withBase('/accessibility/')}>Accessibility Guide</a> for shared conventions.

## Content

Keep option labels short and parallel. Use a placeholder `<option value="">` (e.g. "Select a
fruit…") only when no default is appropriate; otherwise pre-select the most common choice.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'label', type: 'string', description: 'Visible label, associated with the select via Radix Label.' },
        { name: 'error', type: 'string', description: 'Error message. When set, the field is marked invalid and the text announced.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Disables interaction.' },
        { name: 'children', type: 'ReactNode', description: 'The <option> (and <optgroup>) elements, exactly as for a native <select>.' },
        { name: '...rest', type: "SelectHTMLAttributes (minus 'size')", description: 'All other native <select> attributes (value, defaultValue, onChange, name, …) pass through.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'label', type: 'string', description: 'Optional visible label; also the accessible name of the slotted <select>.' },
        { name: 'invalid', type: 'boolean', default: 'false', description: 'Reflected attribute; paints the danger stroke and sets aria-invalid on the <select>.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Reflected attribute; disables and mutes the control (propagated to the slotted <select>).' },
        { name: '(default slot)', type: 'slot', description: 'A native <select> (with its <option>s).' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'items', type: '{ label; value; enabled? }[]', description: 'Data-driven options (React Native has no <option> children). enabled: false only dims the text — the native picker has no per-item disabled.' },
        { name: 'selectedValue', type: 'string', description: 'Currently selected value.' },
        { name: 'onValueChange', type: '(value: string) => void', description: 'Fires with the chosen value.' },
        { name: 'placeholder', type: 'string', description: 'Shown as a leading, empty-valued option.' },
        { name: 'label', type: 'string', description: 'Visible label rendered above the control.' },
        { name: 'error', type: 'string', description: 'Error message; marks the field invalid and announces the text.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Disables the control and applies the disabled palette.' },
        { name: 'accessibilityLabel / testID', type: 'string', description: 'Accessible name for the picker, and a test handle.' },
      ],
    },
  ]}
/>

> On React Native, `NativeSelect` wraps `@react-native-picker/picker`, an **optional peer
> dependency** — install it in your app with `pnpm add @react-native-picker/picker`.

## Code examples

```tsx
// React — @ghds/react
import { NativeSelect } from '@ghds/react/native-select';

// <option> children + onChange + error string
<NativeSelect label="Fruit" error="Please choose a fruit." onChange={(e) => setValue(e.target.value)}>
  <option value="">Select a fruit…</option>
  <option value="apple">Apple</option>
  <option value="banana">Banana</option>
  <option value="cherry">Cherry</option>
</NativeSelect>;
```

```html
<!-- Web Components — @ghds/web-components -->
<script type="module">
  import '@ghds/web-components/native-select';
</script>
<!-- Slot a real <select>; `invalid` (boolean), not an error string -->
<gh-native-select label="Fruit" invalid>
  <select>
    <option value="">Select a fruit…</option>
    <option value="apple">Apple</option>
    <option value="banana">Banana</option>
    <option value="cherry">Cherry</option>
  </select>
</gh-native-select>
```

```tsx
// React Native — @ghds/react-native — requires the picker peer dependency:
//   pnpm add @react-native-picker/picker
import { NativeSelect } from '@ghds/react-native/native-select';

// Data-driven via items[] + selectedValue/onValueChange (no <option> children)
<NativeSelect
  label="Fruit"
  placeholder="Select a fruit…"
  selectedValue={value}
  onValueChange={setValue}
  items={[
    { label: 'Apple', value: 'apple' },
    { label: 'Banana', value: 'banana' },
    { label: 'Cherry', value: 'cherry' },
  ]}
/>;
```

## Live Demo

React and Web Components render live below, driven by the same tokens — open either to see the
native OS picker, and toggle dark mode in the header to see both respond identically. React exposes
the message as an `error` string; Web Components expose the boolean `invalid` state. React Native
can't run in a browser without additional `react-native-web` wiring (not set up in this site), so
its usage is shown as a code sample instead — and it is **data-driven** (an `items` array) rather
than taking `<option>` children.

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <NativeSelectDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <NativeSelectDemoLit />
  </div>
</div>