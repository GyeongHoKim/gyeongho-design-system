---
layout: ../../../layouts/MarkdownLayout.astro
title: Menu
description: A hand-drawn dropdown menu of actions triggered by a button.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import KeyboardTable from '../../../components/KeyboardTable.astro';
import MenuDemoReact from '../../../components/demos/MenuDemo.tsx';
import MenuDemoLit from '../../../components/demos/MenuDemo.astro';
import { withBase } from '../../../lib/withBase';

## Overview

A `Menu` (dropdown) presents a list of **actions** triggered by a button — Edit, Duplicate, Delete.
For choosing a persistent value from a list, use <a href={withBase('/components/select/')}>Select</a>
instead.

## Anatomy

A `<button>` trigger (`aria-haspopup="menu"`) opens a floating `role="menu"` of `role="menuitem"`s.
Both the trigger and the panel are sketchy boxes (`@ghds/sketch-core`). Data-driven — pass an
`items` array of `{ value, label, disabled? }`.

## Variants & States

<StateList
  states={[
    { name: 'Closed', present: true, note: 'Default; aria-expanded=false.' },
    { name: 'Open', present: true, note: 'Panel shown; focus moves into the items.' },
    { name: 'Highlighted item', present: true, note: 'The focused menuitem is highlighted.' },
    { name: 'Disabled item', present: true, note: 'Per-item; skipped by keyboard.' },
  ]}
/>

## Usage

<DoDont
  do={[
    'Use it for actions on an object, not for form value selection (use Select).',
    'Keep labels action-oriented (verbs).',
    'Group destructive actions carefully and consider a confirmation for irreversible ones.',
  ]}
  dont={[
    "Don't overload one menu with many unrelated actions.",
    "Don't use a Menu where a couple of visible buttons would be clearer.",
  ]}
/>

## Accessibility

- **React / Web Components**: `aria-haspopup="menu"` + `aria-expanded` trigger; `role="menu"` panel of
  `role="menuitem"`s. Enter/Space/ArrowDown open and focus the first item; Arrow/Home/End move focus;
  Enter/Space activate; Escape closes and restores focus to the trigger; click-outside closes. Web
  Components dispatches a `select` event.
- **React Native**: a `Modal`-anchored panel; selection by tap. `accessibilityRole="menu"`/`"menuitem"`.
  Arrow-key roving is web-only.
- See the <a href={withBase('/accessibility/')}>Accessibility Guide</a>.

<KeyboardTable
  rows={[
    { keys: 'Enter / Space / ↓', action: 'Open the menu and focus the first item.' },
    { keys: 'Arrow Up / Down', action: 'Move focus between items (wraps).' },
    { keys: 'Home / End', action: 'Focus the first / last item.' },
    { keys: 'Enter / Space', action: 'Activate the focused item.' },
    { keys: 'Escape', action: 'Close and return focus to the trigger.' },
  ]}
/>

## Content

Action labels (verbs). Keep them short.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'label', type: 'string', description: 'Trigger button label.' },
        { name: 'items', type: 'MenuItem[]', description: '{ value, label, disabled? } per entry.' },
        { name: 'onSelect', type: '(value: string) => void', description: 'Fires with the activated item value.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Disables the whole menu.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'items', type: 'GhMenuItem[]', description: '{ value, label, disabled? } (property).' },
        { name: 'label', type: 'string', description: 'Trigger label.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Disables the whole menu.' },
        { name: 'select event', type: 'CustomEvent<string>', description: 'Dispatched with the activated value.' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'label', type: 'string', description: 'Trigger button label.' },
        { name: 'items', type: 'MenuItem[]', description: '{ value, label, disabled? } per entry.' },
        { name: 'onSelect', type: '(value: string) => void', description: 'Fires with the activated item value.' },
        { name: 'disabled', type: 'boolean', default: 'false', description: 'Disables the whole menu.' },
        { name: 'testID', type: 'string', description: 'Test handle for queries.' },
      ],
    },
  ]}
/>

## Live Demo

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <MenuDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <MenuDemoLit />
  </div>
</div>

```tsx
// React Native
import { Menu } from '@ghds/react-native/menu';

<Menu
  label="Actions"
  items={[{ value: 'edit', label: 'Edit' }, { value: 'delete', label: 'Delete' }]}
  onSelect={(value) => run(value)}
/>;
```
