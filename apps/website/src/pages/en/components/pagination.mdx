---
layout: ../../../layouts/MarkdownLayout.astro
title: Pagination
description: A hand-drawn pager for splitting long lists across numbered pages.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import KeyboardTable from '../../../components/KeyboardTable.astro';
import PaginationDemoReact from '../../../components/demos/PaginationDemo.tsx';
import PaginationDemoLit from '../../../components/demos/PaginationDemo.astro';
import { withBase } from '../../../lib/withBase';

## Overview

`Pagination` splits a long list of results across numbered pages, with Previous/Next controls and an
ellipsis that collapses large ranges. It is controlled — you own the current `page` and update it
from `onPageChange`.

## Anatomy

A `<nav>` landmark wrapping Previous, a run of numbered page buttons, and Next. Each button is a
sketchy box (`@ghds/sketch-core`); the current page is filled and marked `aria-current="page"`.
Large gaps collapse to a single ellipsis, keeping the first, last, and the pages around the current
one (`siblingCount`).

## Variants & States

<StateList
  states={[
    { name: 'Current page', present: true, note: 'Filled + aria-current="page".' },
    { name: 'Disabled', present: true, note: 'Previous on the first page and Next on the last.' },
    { name: 'Ellipsis', present: true, note: 'Collapses a run of hidden pages; a single hidden page is shown instead.' },
    { name: 'Hover / Focus', present: true, note: 'Standard button focus ring; keyboard operable.' },
  ]}
/>

## Usage

<DoDont
  do={[
    'Keep it controlled: store the page and update it in onPageChange.',
    'Tune siblingCount for how many neighbours to show around the current page.',
    'Pair it with a results summary ("Showing 21–40 of 200") for context.',
  ]}
  dont={[
    "Don't use pagination for continuous feeds — infinite scroll or a Load more button fits better.",
    "Don't hide Previous/Next; disable them at the ends instead.",
  ]}
/>

## Accessibility

- **React / Web Components**: a `<nav aria-label="Pagination">` landmark; each control is a real
  `<button>` with a descriptive `aria-label` ("Page 3", "Previous page"); the current page carries
  `aria-current="page"`. Web Components dispatches a `page-change` event.
- **React Native**: the row exposes `role="navigation"`; each control is `accessibilityRole="button"`
  with a label and `accessibilityState` (`selected` for the current page, `disabled` for the ends).
- Every control is keyboard-operable on the web platforms.

<KeyboardTable
  rows={[
    { keys: 'Tab', description: 'Move between the pagination controls.' },
    { keys: 'Enter / Space', description: 'Activate the focused control.' },
  ]}
/>

## Content

Numeric page labels, plus "Previous"/"Next" accessible labels. No visible prose beyond the numbers
and chevron icons.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'count', type: 'number', description: 'Total number of pages.' },
        { name: 'page', type: 'number', description: 'Current page (1-based).' },
        { name: 'onPageChange', type: '(page: number) => void', description: 'Fires with the next page.' },
        { name: 'siblingCount', type: 'number', default: '1', description: 'Pages shown on each side of the current one.' },
        { name: 'label', type: 'string', default: "'Pagination'", description: 'Accessible name for the nav landmark.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'count', type: 'number', description: 'Total number of pages.' },
        { name: 'page', type: 'number', description: 'Current page (1-based).' },
        { name: 'siblingCount', type: 'number', default: '1', description: 'Pages shown on each side.' },
        { name: 'label', type: 'string', default: "'Pagination'", description: 'Accessible name.' },
        { name: 'page-change event', type: 'CustomEvent<number>', description: 'Dispatched with the next page.' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'count', type: 'number', description: 'Total number of pages.' },
        { name: 'page', type: 'number', description: 'Current page (1-based).' },
        { name: 'onPageChange', type: '(page: number) => void', description: 'Fires with the next page.' },
        { name: 'siblingCount', type: 'number', default: '1', description: 'Pages shown on each side.' },
        { name: 'label', type: 'string', default: "'Pagination'", description: 'Accessible name.' },
        { name: 'testID', type: 'string', description: 'Test handle for queries.' },
      ],
    },
  ]}
/>

## Live Demo

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <PaginationDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <PaginationDemoLit />
  </div>
</div>

```tsx
// React Native
import { Pagination } from '@ghds/react-native/pagination';

const [page, setPage] = useState(1);
<Pagination count={12} page={page} onPageChange={setPage} />;
```
