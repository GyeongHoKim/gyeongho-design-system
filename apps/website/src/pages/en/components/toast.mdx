---
layout: ../../../layouts/MarkdownLayout.astro
title: Toast
description: A hand-drawn transient notification that floats and auto-dismisses.
---

import PropsTable from '../../../components/PropsTable.astro';
import StateList from '../../../components/StateList.astro';
import DoDont from '../../../components/DoDont.astro';
import ToastDemoReact from '../../../components/demos/ToastDemo.tsx';
import ToastDemoLit from '../../../components/demos/ToastDemo.astro';
import { withBase } from '../../../lib/withBase';

## Overview

A `Toast` is a brief, transient notification that floats above the page (bottom-right) and
auto-dismisses. Use it for confirmations and low-priority feedback; for persistent, in-layout status
use an <a href={withBase('/components/alert/')}>Alert</a>.

## Anatomy

A sketchy, elevated box (`@ghds/sketch-core`) fixed to the bottom-right, with a severity icon, an
optional title, a message, and a dismiss button. It auto-dismisses after `duration` (ms; `0` to
persist). Controlled via `open`/`onClose`; render several to stack them.

## Variants & States

<StateList
  states={[
    { name: 'Info / Success / Warning / Danger', present: true, note: 'Severity colour + icon.' },
    { name: 'Auto-dismiss', present: true, note: 'Closes after duration (default 5000ms); 0 persists.' },
    { name: 'Manual dismiss', present: true, note: 'Always has a close button.' },
  ]}
/>

## Usage

<DoDont
  do={[
    'Keep messages short — a toast is glanceable.',
    'Use danger toasts sparingly; consider an inline Alert for errors that need action.',
    'Own the open state and clear it in onClose.',
  ]}
  dont={[
    "Don't put essential actions only in a toast — it disappears.",
    "Don't show many toasts at once; queue them.",
  ]}
/>

## Accessibility

- **All platforms**: `danger` toasts use `role="alert"` (assertive); the rest `role="status"`
  (polite) / an `aria-live` region, so they are announced without stealing focus. A labelled
  "Dismiss" button is always present.
- **React Native**: rendered in a transparent `Modal` with `pointerEvents="box-none"` so taps outside
  the toast pass through.
- See the <a href={withBase('/accessibility/')}>Accessibility Guide</a>.

## Content

A short message and optional title. The auto-dismiss default is 5000ms (a behavioural timing, not a
motion token); pass `duration={0}` to keep it until dismissed.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'open', type: 'boolean', description: 'Whether the toast is shown.' },
        { name: 'onClose', type: '() => void', description: 'Called on auto-dismiss or close button.' },
        { name: 'variant', type: "'info' | 'success' | 'warning' | 'danger'", default: "'info'", description: 'Severity.' },
        { name: 'title', type: 'ReactNode', description: 'Optional bold title.' },
        { name: 'children', type: 'ReactNode', description: 'Message body.' },
        { name: 'duration', type: 'number', default: '5000', description: 'Auto-dismiss ms; 0 to persist.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'open', type: 'boolean', description: 'Whether the toast is shown (reflected).' },
        { name: 'variant', type: "'info' | 'success' | 'warning' | 'danger'", default: "'info'", description: 'Severity (reflected).' },
        { name: 'heading', type: 'string', description: 'Optional bold title.' },
        { name: 'duration', type: 'number', default: '5000', description: 'Auto-dismiss ms; 0 to persist.' },
        { name: 'close event', type: 'CustomEvent', description: 'Dispatched on auto-dismiss / close.' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'open', type: 'boolean', description: 'Whether the toast is shown.' },
        { name: 'onClose', type: '() => void', description: 'Called on auto-dismiss or close button.' },
        { name: 'variant', type: "'info' | 'success' | 'warning' | 'danger'", default: "'info'", description: 'Severity.' },
        { name: 'title', type: 'string', description: 'Optional bold title.' },
        { name: 'children', type: 'ReactNode', description: 'Message body.' },
        { name: 'duration', type: 'number', default: '5000', description: 'Auto-dismiss ms; 0 to persist.' },
        { name: 'testID', type: 'string', description: 'Test handle for queries.' },
      ],
    },
  ]}
/>

## Code examples

```tsx
// React — @ghds/react (imperative toast() + a Toaster viewport)
import { Toaster, toast } from '@ghds/react/toast';

// Mount the viewport once, high in the tree:
<Toaster position="bottom-right" />;

// Then trigger toasts imperatively:
toast.success('Your changes have been saved.', { title: 'Saved' });
```

```html
<!-- Web Components — @ghds/web-components -->
<script type="module">
  import '@ghds/web-components/toast';
</script>
<gh-toast id="my-toast" variant="success" heading="Saved" duration="0">
  Your changes have been saved.
</gh-toast>
<script>
  const el = document.getElementById('my-toast');
  el.open = true;
  el.addEventListener('close', () => { el.open = false; });
</script>
```

```tsx
// React Native — @ghds/react-native
import { Toast } from '@ghds/react-native/toast';

<Toast open={open} onClose={() => setOpen(false)} variant="success" title="Saved">
  Your changes have been saved.
</Toast>;
```

## Live Demo

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <ToastDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <ToastDemoLit />
  </div>
</div>
