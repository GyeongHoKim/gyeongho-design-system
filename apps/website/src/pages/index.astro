---
// Root redirect page. Astro's i18n `redirectToDefaultLocale` generates a
// redirect from `/` to `/en/`, but still requires a root index page to exist.
return Astro.redirect(`${import.meta.env.BASE_URL}en/`);
---
