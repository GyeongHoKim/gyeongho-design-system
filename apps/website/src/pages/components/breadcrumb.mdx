---
layout: ../../layouts/MarkdownLayout.astro
title: Breadcrumb
description: A hand-drawn breadcrumb navigation trail showing the path to the current page.
---

import PropsTable from '../../components/PropsTable.astro';
import StateList from '../../components/StateList.astro';
import DoDont from '../../components/DoDont.astro';
import BreadcrumbDemoReact from '../../components/demos/BreadcrumbDemo.tsx';
import BreadcrumbDemoLit from '../../components/demos/BreadcrumbDemo.astro';
import { withBase } from '../../lib/withBase';

## Overview

A `Breadcrumb` shows where the current page sits in the site hierarchy and lets people jump back up
the trail. It is a secondary navigation aid — not a replacement for primary navigation.

## Anatomy

A `<nav>` landmark wrapping an ordered list: each ancestor is a link, separated by a hand-drawn
chevron, and the last entry is the current page (not a link). The API is **data-driven** on every
platform — pass an `items` array of `{ label, href? }`.

## Variants & States

<StateList
  states={[
    { name: 'Link', present: true, note: 'Every item except the last is an activatable link.' },
    { name: 'Current page', present: true, note: 'The last item is marked aria-current="page" and is not a link.' },
    { name: 'Hover', present: true, note: 'Links shift to the primary text colour on hover (web).' },
    { name: 'Disabled', present: false },
  ]}
/>

## Usage

<DoDont
  do={[
    'Order items from the site root to the current page.',
    'Keep labels short — match the page titles they link to.',
    'Use it for hierarchy, not for step-by-step flows (use a stepper/wizard for those).',
  ]}
  dont={[
    "Don't make the current page a link — it is the page you are on.",
    "Don't use a breadcrumb as the only way to navigate; it complements primary nav.",
  ]}
/>

## Accessibility

- **React / Web Components**: a `<nav aria-label="Breadcrumb">` landmark wraps an `<ol>`; the current
  page carries `aria-current="page"`. Web Components dispatches a `select` event on link activation.
- **React Native**: the row exposes `role="navigation"` with the label; items are `accessibilityRole="link"`.
  There is no `href` on React Native — handle navigation in the `onSelect` callback.
- See the <a href={withBase('/accessibility/')}>Accessibility Guide</a>.

## Content

Short, recognisable labels that mirror the destination page titles. The last item names the current
page.

## Props API

<PropsTable
  groups={[
    {
      platform: 'React',
      packageName: '@ghds/react',
      rows: [
        { name: 'items', type: 'BreadcrumbItem[]', description: 'Trail of { label, href? }; last item is the current page.' },
        { name: 'label', type: 'string', default: "'Breadcrumb'", description: 'Accessible name for the nav landmark.' },
        { name: 'onSelect', type: '(item, index) => void', description: 'Fires when a link is activated.' },
      ],
    },
    {
      platform: 'Web Components',
      packageName: '@ghds/web-components',
      rows: [
        { name: 'items', type: 'GhBreadcrumbItem[]', description: 'Property (not attribute) — set via JS.' },
        { name: 'label', type: 'string', default: "'Breadcrumb'", description: 'Accessible name for the nav landmark.' },
        { name: 'select event', type: 'CustomEvent<{ item, index }>', description: 'Dispatched on link activation.' },
      ],
    },
    {
      platform: 'React Native',
      packageName: '@ghds/react-native',
      rows: [
        { name: 'items', type: 'BreadcrumbItem[]', description: 'Trail of { label, href? }.' },
        { name: 'label', type: 'string', default: "'Breadcrumb'", description: 'Accessible name.' },
        { name: 'onSelect', type: '(item, index) => void', description: 'Navigate here — there is no href on RN.' },
        { name: 'testID', type: 'string', description: 'Test handle for queries.' },
      ],
    },
  ]}
/>

## Live Demo

<div class="demo-columns">
  <div class="demo-column">
    <h3>React <span class="demo-tag">@ghds/react</span></h3>
    <BreadcrumbDemoReact client:only="react" />
  </div>
  <div class="demo-column">
    <h3>Web Components <span class="demo-tag">@ghds/web-components</span></h3>
    <BreadcrumbDemoLit />
  </div>
</div>

```tsx
// React Native
import { Breadcrumb } from '@ghds/react-native';

<Breadcrumb
  items={[{ label: 'Home', href: '/' }, { label: 'Data' }]}
  onSelect={(item) => navigate(item.href)}
/>;
```
